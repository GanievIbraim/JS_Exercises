// получаем элемент абзаца по id
const elem = document.getElementById('elem');

// привязываем функции к элементу
elem.addEventListener('click', func1);
elem.addEventListener('click', func2);
elem.addEventListener('click', func3);
